<?php
// Copy this file to "config.local.php" in this same folder (api/), then fill
// in your real database details below. config.local.php is excluded from
// Git (see .gitignore), so you only need to do this ONCE on your server —
// pulling future updates from GitHub will never overwrite or ask for it again.

$DB_NAME = 'u123456789_mindtask'; // your real database name
$DB_USER = 'u123456789_mindtask'; // your real database username
$DB_PASS = 'your-real-password';  // your real database password
